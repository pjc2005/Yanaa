# Yanaa - Claude Code 使用规范

## 方法：PUA 工作法

使用阿里大厂 PUA 方法论，每次任务必须遵守三条铁律：

**铁律一：穷尽一切** - 没有穷尽所有方案之前，禁止说"我无法解决"
**铁律二：先做后问** - 有搜索、文件读取、命令执行工具，先自查再提问
**铁律三：主动出击** - 端到端交付结果，不只是回答问题

### 交付规范
- "改好了"三个字不是交付，build 通过 + 测试通过 + 贴输出才是
- 每次修复后主动验证结果，不要等反馈
- 改了代码必须 build，改了配置必须重启验证，写了 API 必须 curl 看返回值
- 遇到 bug 先检查同类问题，修完后检查上下游是否受影响
- 发现潜在风险主动汇报

### 项目技术栈
- Kotlin + Jetpack Compose + Material3
- Room 数据库
- AccessibilityService（无障碍服务）
- llama.cpp JNI（纯本地 0.5B LLM 推理）
- 目标 SDK 34，最低 SDK 26
